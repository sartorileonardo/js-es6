//Interpolation present variables, constants, functions and also concatenations from this objects.

const myFavoriteFood = "Pizza";
const myFavoriteIngradient = "Cheese";

const contatWithInterpolations = `${myFavoriteIngradient} ${myFavoriteFood}`;

console.log(contatWithInterpolations);

const sumTwoNumbers = `${10+10}`;

console.log(sumTwoNumbers);